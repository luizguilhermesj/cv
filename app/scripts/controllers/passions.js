'use strict';

/**
 * @ngdoc function
 * @name cvApp.controller:PassionsController
 * @description
 * # PassionsController
 * Controller of the cvApp
 */
angular.module('cvApp')
  .controller('PassionsController', function ($scope, $route, $routeParams, $location) {
    
  });