'use strict';

/**
 * @ngdoc function
 * @name cvApp.controller:FindMeController
 * @description
 * # FindMeController
 * Controller of the cvApp
 */
angular.module('cvApp')
  .controller('FindMeController', function ($scope, $route, $routeParams, $location) {
    
  });